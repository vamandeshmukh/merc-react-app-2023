import Child from './Child';

const Parent = () => {

    return (
        <div>
            <h1>Parent Component</h1>
            <p>Parent</p>
            <Child />
        </div>
    );


};

export default Parent;